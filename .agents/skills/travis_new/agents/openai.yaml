interface:
  display_name: "Travis New"
  short_description: "Repo-specific patterns and workflows for travis_new"
  default_prompt: "Use the travis_new repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true